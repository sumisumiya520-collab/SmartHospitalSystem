services:
  - type: web
    name: smart-hospital
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Development # TEMPORARY: full stack trace for debugging — revert to Production after fix
      - key: DATABASE_URL
        sync: false
